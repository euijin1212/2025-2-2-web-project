<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- 
        EJS에서는 이런 식으로 변수를 받아와서 페이지마다 다른 제목을 쓸 수 있습니다.
        (예: app.js에서 res.render('login', { pageTitle: '로그인' }) )
    -->
    <title><%= (typeof pageTitle !== 'undefined' ? pageTitle : 'StudyLink') %> - 📚 온라인 독서 소모임</title>
    
    <!-- 1. Bootstrap CSS (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- 2. Google Noto Sans 폰트 (선택 사항) -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">
    
    <!-- 3. 공통 커스텀 CSS 파일 링크 (app.js의 express.static 설정에 따라 /css/style.css로 경로를 잡습니다) -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<!-- === (분리된) 공통 네비게이션 바 === -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="studies.html">📚 온라인 독서 소모임</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
            <li class="nav-item">
                <a class="nav-link" href="studies.html">스터디 찾기</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="study-form.html">스터디 만들기</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="mypage.html">마이페이지</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="login.html">로그인</a>
            </li>
            </ul>
        </div>
    </div>
</nav>

<!-- === 본문 시작 === -->
<main class="container">