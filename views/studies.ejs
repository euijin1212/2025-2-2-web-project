<%- include('partials/header') %>

<div class="mb-4 p-4 bg-light rounded-3">
    <h2>스터디 찾기</h2>
    <p>관심 있는 주제의 독서 모임에 참여해보세요.</p>
        
    <form action="/studies" method="GET" class="row g-3 align-items-end">
        <div class="col-md-5">
            <label for="search-keyword" class="form-label">주제 (책, 스터디 제목)</label>
            <input type="text" class="form-control" id="search-keyword" name="keyword"
                   value="<%= typeof keyword !== 'undefined' ? keyword : '' %>">
        </div>
        <div class="col-md-3">
            <label for="search-day" class="form-label">관심 요일</label>
            <select id="search-day" class="form-select" name="day">
                <option value="" <%= !day ? 'selected' : '' %>>전체</option>
                <option value="mon" <%= day === 'mon' ? 'selected' : '' %>>월</option>
                <option value="tue" <%= day === 'tue' ? 'selected' : '' %>>화</option>
                <option value="wed" <%= day === 'wed' ? 'selected' : '' %>>수</option>
                <option value="thu" <%= day === 'thu' ? 'selected' : '' %>>목</option>
                <option value="fri" <%= day === 'fri' ? 'selected' : '' %>>금</option>
                <option value="sat" <%= day === 'sat' ? 'selected' : '' %>>토</option>
                <option value="sun" <%= day === 'sun' ? 'selected' : '' %>>일</option>
            </select>
        </div>
        <div class="col-md-2 d-grid">
            <button type="submit" class="btn btn-primary">검색</button>
        </div>
    </form>
</div>

<div class="row row-cols-1 row-cols-md-3 g-4">
    <% if (studies && studies.length > 0) { %>
        <% studies.forEach(study => { %>
            <div class="col">
                <div class="card h-100">
                    <% if (study.bookCoverUrl) { %>
                        <img src="<%= study.bookCoverUrl %>" class="card-img-top" alt="책 표지">
                    <% } %>

                    <div class="card-body">
                        <h5 class="card-title"><%= study.title %></h5>
                        <p class="card-text">
                            <%= (study.description || '').length > 80 
                                ? study.description.substring(0, 80) + '...' 
                                : (study.description || '') %>
                        </p>
                        <% if (study.bookTitle) { %>
                            <span class="badge bg-secondary mb-2"><%= study.bookTitle %></span>
                        <% } %>
                    </div>

                    <div class="card-footer d-flex justify-content-between align-items-center">
                        <span class="text-muted small">
                            <% if (study.day) { %>
                                <% if (study.day === 'mon') { %>월<% } %>
                                <% if (study.day === 'tue') { %>화<% } %>
                                <% if (study.day === 'wed') { %>수<% } %>
                                <% if (study.day === 'thu') { %>목<% } %>
                                <% if (study.day === 'fri') { %>금<% } %>
                                <% if (study.day === 'sat') { %>토<% } %>
                                <% if (study.day === 'sun') { %>일<% } %>
                            <% } %>
                        </span>

                        <a href="/studies/<%= study.id %>" class="btn btn-outline-primary btn-sm">
                            상세보기
                        </a>
                    </div>
                </div>
            </div>
        <% }) %>
    <% } else { %>
        <div class="alert alert-secondary">등록된 스터디가 없습니다.</div>
    <% } %>
</div>

<%- include('partials/footer') %>
